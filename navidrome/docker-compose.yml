services:
  navidrome:
    image: deluan/navidrome:latest
    container_name: navidrome
    restart: unless-stopped
    volumes:
      - ./data:/data
      - ./music:/music:ro
    ports:
      - 4533:4533
