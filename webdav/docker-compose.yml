services:
  webdav:
    image: bytemark/webdav:latest
    container_name: webdav
    restart: unless-stopped
    volumes:
      - ./dav:/var/lib/dav
    ports:
      - 80:80
    env_file:
      - .env
