services:
  webdav:
    image: bytemark/webdav:latest
    container_name: webdav
    restart: unless-stopped
    ports:
      - 80:80
    volumes:
      - ./dav:/var/lib/dav
    env_file:
      - .env
